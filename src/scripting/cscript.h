#pragma once
#include <filesystem>
#include <string>
#include <angelscript.h>
#include "scripting/helpers/clogger.h"

namespace ha
{
namespace scripting
{

// class CScript
// {
// public:
//   CScript(const std::filesystem::path &file);
//   ~CScript();

//   // bool operator==(const CScript& other) const;
//   const std::filesystem::path &file() const { return m_file; }
//   // void initialize();
//   void callMethod(const std::string &method);

// private:
//   std::filesystem::path m_file;
//   std::string m_name;
//   asIScriptContext *m_context;
//   asIScriptModule *m_module;

//   std::string content();
//   void callDestroySelf(const std::string &message);


// };

}
}
