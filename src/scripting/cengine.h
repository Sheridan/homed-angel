#pragma once
#include <angelscript.h>
#include <vector>
#include "scripting/cscript.h"


namespace ha
{
namespace scripting
{

// class CEngine
// {
// public:
//    CEngine();
//   ~CEngine();

//   // void attachScript(const std::filesystem::path &file);
//   // void detachScript(const std::filesystem::path &file);
//   // asIScriptEngine *asEngine() { return m_asEngine; }

// private:
//   // asIScriptEngine* m_asEngine;
//   // std::vector<CScript *> m_scripts;
//   // ha::scripting::helpers::CLogger *m_logger;
//   // void messageCallback(const asSMessageInfo* msg);



//   // CScript *script(const std::filesystem::path &file);
// };

// #define HA_ACCERT_CALL(_what) { int r = _what; assert(r >= 0); }

}
}
